export * from './list-item';
export * from './service';
export * from './types';

/*
  INSERT
  fetchFrappe('/api/resource/Patient', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({}),
  })

  - UPDATE
  fetchFrappe(`/api/resource/Patient/${data.name}`, {
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({}),
  })
  */
